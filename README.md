# Task Manager

Projeto base em HTML, CSS e JavaScript puros.

## Como rodar

Basta abrir o arquivo `index.html` no navegador.

Alternativamente, sirva a pasta com um servidor estático, por exemplo:

```bash
npx serve .
```
